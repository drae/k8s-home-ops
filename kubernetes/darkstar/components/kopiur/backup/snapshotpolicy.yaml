---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kopiur.home-operations.com/snapshotpolicy_v1alpha1.json
apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotPolicy
metadata:
  name: ${APP}
spec:
  credentialProjection:
    enabled: true
  # restic (VolSync's mover) silently tolerates unreadable files; kopia treats
  # one as fatal by default. Match restic's leniency so a stray root-owned
  # dotfile in an app's config PVC doesn't fail the whole snapshot.
  errorHandling:
    ignoreFileErrors: true
  mover:
    securityContext:
      runAsUser: ${APP_UID:-2000}
      runAsGroup: ${APP_GID:-2000}
  repository:
    kind: ClusterRepository
    name: nas
  retention:
    keepHourly: 24
    keepDaily: 7
  sources:
    - pvc:
        name: "${KOPIUR_CLAIM:-${APP}}"
  volumeSnapshotClassName: ${KOPIUR_SNAPSHOTCLASS:-topolvm-thin-provisioner}
