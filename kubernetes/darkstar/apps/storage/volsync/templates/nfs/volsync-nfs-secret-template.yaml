
---
# yaml-language-server: $schema=https://kubernetes-schemas-bki.pages.dev/secretgen.carvel.dev/secrettemplate_v1alpha1.json
apiVersion: secretgen.carvel.dev/v1alpha1
kind: SecretTemplate
metadata:
  name: ${APP}-volsync-nfs
spec:
  inputResources:
    - name: template
      ref:
        apiVersion: v1
        kind: Secret
        name: volsync-nfs-secret
  template:
    stringData:
      RESTIC_REPOSITORY: $(.template.data.repository-template)/${APP}
      RESTIC_PASSWORD: $(.template.data.restic-password)
