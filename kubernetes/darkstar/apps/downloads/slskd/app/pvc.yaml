---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slskd-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: topolvm-thin-provisioner
