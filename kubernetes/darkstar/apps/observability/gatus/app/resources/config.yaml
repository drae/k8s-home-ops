---
# Note: Gatus vars should be escaped with $${VAR_NAME} to avoid interpolation by Flux
web:
  port: ${WEB_PORT}
storage:
  type: sqlite
  path: ${GATUS_CONFIG_PATH}/gatus.db
metrics: true
ui:
  title: Health Status
  header: Health Status
  logo: https://avatars.githubusercontent.com/u/449736
connectivity:
  checker:
    target: 1.1.1.1:53
    interval: 1m
