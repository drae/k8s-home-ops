---
originRequest:
  originServerName: ingress.${SECRET_DOMAIN}

ingress:
  - hostname: "${SECRET_DOMAIN}"
    service: &svc https://ingress-nginx-ext-controller.networking.svc.cluster.local:443
  - hostname: "*.${SECRET_DOMAIN}"
    service: *svc
  - service: http_status:404
