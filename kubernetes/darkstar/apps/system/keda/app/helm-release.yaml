---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: keda
spec:
  interval: 1h
  chart:
    spec:
      chart: keda
      version: 2.18.1
      interval: 1h
      sourceRef:
        kind: HelmRepository
        name: keda-charts
        namespace: flux-system
  values:
    enableServiceLinks: false
