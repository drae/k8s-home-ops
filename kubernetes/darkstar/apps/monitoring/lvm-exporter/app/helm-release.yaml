---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/app-template-3.0.1/charts/other/app-template/schemas/helmrelease-helm-v2beta2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: lvm-exporter
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.6.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
      interval: 30m
  upgrade:
    cleanupOnFail: true
  values:
    controllers:
      lvm-exporter:
        containers:
          app:
            image:
              repository: ghcr.io/drae/lvm-exporter
              tag: main@sha256:2d5c8ed45eabfc673b4310c6f8583b0005ad183b6b3e7afb1c48300038265be8
            securityContext:
              privileged: true
    service:
      app:
        controller: lvm-exporter
        ports:
          http:
            port: 9845
    serviceMonitor:
      app:
        serviceName: lvm-exporter
        endpoints:
          - port: http
            scheme: http
            path: /metrics
            interval: 1m
            scrapeTimeout: 10s
