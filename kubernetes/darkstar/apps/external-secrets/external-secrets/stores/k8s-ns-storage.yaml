apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: k8s-ns-storage
spec:
  provider:
    kubernetes:
      remoteNamespace: storage
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
