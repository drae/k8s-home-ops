---
apiVersion: v1
kind: Secret
metadata:
  name: tautulli-restic-secret
  namespace: media
stringData:
  RESTIC_REPOSITORY: "${RESTIC_REPOSITORY}/tautulli"
  RESTIC_PASSWORD: ${RESTIC_PASSWORD}
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: tautulli
  namespace: media
spec:
  sourcePVC: tautulli-config
  trigger:
    schedule: "0 5 * * *"
  restic:
    cacheCapacity: 3G
    copyMethod: Snapshot
    pruneIntervalDays: 7
    repository: tautulli-restic-secret
    moverSecurityContext:
      runAsUser: 568
      runAsGroup: 568
      fsGroup: 568
    retain:
      daily: 7
      within: 3d
    volumeSnapshotClassName: zfs-local-dataset-snap
    capacity: 10Gi
    storageClassName: zfs-local-dataset
    cacheStorageClassName: local-hostpath
