---
apiVersion: v1
kind: Service
metadata:
  name: &app sabnzbd
  namespace: downloads
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
spec:
  type: ExternalName
  externalName: apollo.${SECRET_DOMAIN_LAN}
  ports:
    - name: http
      port: &port 8080
      protocol: TCP
      targetPort: *port
