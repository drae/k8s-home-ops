---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vm-pp-sim01-c-drive
spec:
  storageClassName: zfs-local-dataset
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "200Gi"
