---
all:
  vars:
    ansible_user: localadmin
    ansible_ssh_port: 22
  children:
    nas:
      hosts:
        apollo:
          ansible_host: apollo
    tvh:
      hosts:
        gravity:
          ansible_host: gravity
