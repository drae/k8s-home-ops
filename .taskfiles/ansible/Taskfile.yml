---
version: "3"

tasks:
  init:
    desc: Install/Upgrade required ansible collections
    dir: infrastructure/ansible
    cmds:
      - "ansible-galaxy install -r requirements.yml --force"
      - "ansible-galaxy collection install -r requirements.yml --force"
