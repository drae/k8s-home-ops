---
services:
  dockhand:
    image: docker.io/fnsys/dockhand:v1.0.29
    container_name: dockhand
    restart: unless-stopped
    ports:
      - 3000:3000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - data:/app/data

volumes:
  data: {}
